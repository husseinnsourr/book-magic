# 🧩 Components

مكونات واجهة المستخدم لصفحة BookReader.

## 📁 الملفات

| الملف                  | الوصف          | المسؤولية                   |
| ---------------------- | -------------- | --------------------------- |
| `index.ts`             | تصدير المكونات | نقطة الدخول                 |
| `ReaderHeader.tsx`     | الهيدر         | عنوان الكتاب، زر التحرير    |
| `ReaderDock.tsx`       | شريط الأدوات   | أيقونات الأدوات             |
| `DockPanels.tsx`       | لوحات الأدوات  | Font, Format, Align, Insert |
| `A4Page.tsx`           | صفحة A4        | عرض/تحرير المحتوى           |
| `FloatingControls.tsx` | أزرار عائمة    | Zoom، التنقل                |
| `LoadingStates.tsx`    | حالات التحميل  | Spinner، Progress، Error    |

## 🎨 styles/

أنماط CSS لكل مكون:

```
styles/
├── header.css     # أنماط الهيدر
├── dock.css       # أنماط الـ Dock واللوحات
├── page.css       # أنماط صفحة A4
├── controls.css   # أنماط الأزرار العائمة
└── loading.css    # أنماط التحميل
```

## 📐 المكونات

### ReaderHeader

```tsx
<ReaderHeader book={book} />
```

- عرض عنوان الكتاب والمؤلف
- زر التحرير/القراءة

### ReaderDock

```tsx
<ReaderDock
  activePanel={panel}
  setActivePanel={setPanel}
  language="ar"
  isRTL={true}
  // ... formatting props
/>
```

- يظهر فقط في وضع التحرير
- يتبع اتجاه اللغة

### A4Page

```tsx
<A4Page isEditMode={false} currentContent={content} language="ar" />
```

- وضع القراءة: عرض فقط
- وضع التحرير: contentEditable

### FloatingControls

```tsx
<FloatingControls zoom={100} currentPage={1} totalPages={50} />
```

- Zoom: 50% - 200%
- التنقل بين الصفحات

## ⚠️ ملاحظات

1. **لا تستخدم onClick للتحرير** - تم إزالة click-to-edit لأنه خطير
2. **الـ Dock يظهر فقط في Edit Mode**
3. **جميع النصوص مترجمة** - استخدم `t()` من `i18n.ts`
